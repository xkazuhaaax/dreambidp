{
  "version": 2,
  "buildCommand": "npm run build",
  "env": {
    "NODE_ENV": "production"
  },
  "note": "This project is now configured to use Netlify + Neon database. See NETLIFY_NEON_SETUP.md for deployment instructions. Vercel configuration is kept for reference only.",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/handler.js"
    },
    {
      "source": "/(.*)",
      "destination": "/dist/index.html"
    }
  ]
}